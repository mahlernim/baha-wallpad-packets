# 프로토콜 개요

상위 문서: [README](../README.md)  
바로 가기: [하드웨어](hardware.md) | [거실 조명 `10 04`](lighting-node-10-04.md) | [현관 / 일괄소등 `1F 0F`](master-switch-node-1f0f.md) | [난방 `40 90`](heater-node-40-90.md) | [참고 자료](../reference/README.md)

## 환경

- 첫 성공 세션 날짜: `2026-04-18`
- 캡처 당시 호스트 시간대: `Asia/Seoul`
- 테스트에 사용한 시리얼 포트: `COM5`
- UART 설정: `9600 8N1`

## 프레임 구조

관찰된 패킷은 대체로 다음 구조를 따른다.

```text
02 [node_hi] [node_lo] [opcode/payload...] [xor_checksum] 00
```

현재까지 확인된 프레이밍 규칙:

- 시작 바이트: `02`
- 종료 바이트: `00`
- 마지막에서 두 번째 바이트: 첫 `02`부터 마지막 페이로드 바이트까지의 XOR

예:

```text
02 30 80 01 00 B3 00
02 ^ 30 ^ 80 ^ 01 ^ 00 = B3

02 10 04 02 02 01 01 16 00
02 ^ 10 ^ 04 ^ 02 ^ 02 ^ 01 ^ 01 = 16
```

## 백그라운드 트래픽

유휴 상태에서 반복적으로 관찰된 프레임:

```text
02 30 80 01 00 B3 00
02 31 80 01 00 B2 00
02 1F 0F 01 00 13 00
```

앞의 두 프레임은 더 넓은 RS485 네트워크에서 주기적으로 오가는 하트비트 또는 폴링 계열 트래픽으로 보인다.

## 확인된 노드

| 노드 | 역할 | 조회 경로 | 쓰기 경로 |
| --- | --- | --- | --- |
| `10 04` | 거실 4채널 조명 | `01 -> 81` | `02 02 ...` 조명 마스크 쓰기 |
| `10 06` | 타 설치 사례의 6채널 조명 | `01 -> 81` | `02 02 ...` 조명 마스크 쓰기 |
| `1F 0F` | 현관 / 일괄소등 스위치 상태 노드 | `01 -> 81` | `02 01 <value>` |
| `40 90` | 난방 상태 / 제어 인터페이스 | `01 -> 81`, `05 -> 85` | `02 06 ...` |

## 실무적 해석

- `10 xx` 계열은 채널 수를 `node_lo`로 표현하는 조명 제어 노드다.
- 실측 완료 노드 `10 04`는 거실 4채널 조명 제어 노드다.
- 타 설치 사례에서 확인된 `10 06`은 같은 문법의 6채널 조명 노드다.
- `1F 0F`는 현관 / 일괄소등 브랜치에서 실제 상태와 제어에 관여하는 노드다.
- `40 90`는 BAHA 측에서 보이는 난방 상태 / 제어 노드다.

조명 노드 일반 규칙:

```text
조회: 02 10 <channel_count> 01 00 <xor> 00
응답: 02 10 <channel_count> 81 02 <capability_mask> <state_mask> <xor> 00
쓰기: 02 10 <channel_count> 02 02 <channel_mask> <value_mask> <xor> 00
```

- `channel_count`는 조명 채널 수와 일치한다.
- `capability_mask`는 해당 채널 수만큼의 하위 비트가 1인 값으로 해석한다.
- `channel_mask`와 `value_mask`는 각 조명 채널을 비트로 표현한다.
- 켜기는 `value_mask = channel_mask`, 끄기는 `value_mask = 00` 이다.

각 노드별 상세 패킷 표와 명령 예시는 개별 문서에 정리했다.

## 관련 문서

- 하드웨어 / 사진: [hardware.md](hardware.md)
- 거실 조명: [lighting-node-10-04.md](lighting-node-10-04.md)
- 현관 / 일괄소등: [master-switch-node-1f0f.md](master-switch-node-1f0f.md)
- 난방: [heater-node-40-90.md](heater-node-40-90.md)
